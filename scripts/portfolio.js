function hoverImage(element) {
    if (element) {
        element.style.transform = ""; // Efeito de exemplo
    }
}

function resetImage(element) {
    if (element) {
        element.style.transform = ""; // Resetar efeito
    }
}
